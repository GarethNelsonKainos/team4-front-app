name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
     - uses: actions/checkout@v4
    
     - name: Setup Node.js
       uses: actions/setup-node@v4
       with:
         node-version: '20'
         cache: 'npm'
    
     - name: Install dependencies
       run: npm ci
    
     - name: Run Biome checks
       run: npm run ci:check
    
     - name: Build application
       run: npm run build
    
     # Optional: Add frontend-specific tests when you have them
     #- name: Run tests
     #  run: npm test
     
     # Optional: Deploy step for frontend
     #- name: Deploy
     #  if: github.ref == 'refs/heads/main'
     #  run: npm run deploy